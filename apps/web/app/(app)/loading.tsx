import { LoadingScreen } from '../../components/LoadingScreen';

/** Route-segment loading fallback for the authenticated app (branded splash). */
export default function Loading() {
  return <LoadingScreen />;
}
