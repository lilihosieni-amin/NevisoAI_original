declare module 'tailwindcss-rtl' {
  // Tailwind plugin (logical-property RTL utilities). Typed loosely; the
  // preset only passes it through to Tailwind's plugins array.
  const plugin: unknown;
  export default plugin;
}
