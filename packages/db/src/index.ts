/**
 * @neviso/db — single source of the generated Prisma client.
 *
 * The client is generated into `../generated/client` by `prisma generate`
 * (run `npm run db:generate` from the repo root). All apps import Prisma
 * types/values from here rather than reaching into the generated folder.
 */
export * from '../generated/client';
export { PrismaClient } from '../generated/client';
